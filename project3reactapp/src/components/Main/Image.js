//EACH IMAGE needs to be imported with corresponding alt

import React from "react";

function Image(props) {
    return (
        <img src={INSERT} alt={INSERT} className="img-fluid" />
    );
}

export default Image;